#include <stdio.h>

void sum() {
    int num1, num2, sum;

    // Input two numbers
    printf("\n\nEnter two integers: ");
    scanf("%d %d", &num1, &num2);

    // Calculate the sum
    sum = num1 + num2;

    // Display the result
    printf("Sum = %d\n", sum);

   // return 0;
}

